query CartInput {
  cart {
    lines {
      id
      quantity
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  discount {
    discountClasses
  }
  shop {
    bulk_discounts: metafield(namespace: "custom", key: "bulk_discounts") {
      value
    }
  }
}
